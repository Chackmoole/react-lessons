import React from 'react';
import { StyledBlockWithLine } from './BlockWithLine.styled';

const BlockWithLine = ({ children }) => {
  return <StyledBlockWithLine>{children}</StyledBlockWithLine>;
};

export default BlockWithLine;
